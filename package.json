{
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "latest",
    "@cypress/webpack-preprocessor": "latest",
    "cypress": "latest",
    "webpack": "latest"
  },
  "scripts": {
    "test:chrome": "cypress run --browser chrome",
    "test:watch": "cypress run --browser chrome --headed --no-exit"  
  }
}
